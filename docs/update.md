a04_Schedule_Manager_v3/docs/update.mdの内容に従って更新する

不具合や利用しての改善希望
完了したものは、[ ] を [x]にしてください
しっかりテストをして機能が織り込まれたことを確認してから[x]とすること

[x] 常時の情報欄の横の保存ボタンは不要。自動保存にする
[x] teamの情報欄を今日と常時の列に分けて、両方表示する

[x] システムがdarkモードでもLightモード固定にしてください
[x] メインパネルでは日付で時間帯の行を選択している場合、中央で自身のticketをクリックしたら割り当ててください。なので割り当てというボタンは不要になります
[x] 1日の予定は画面左側に表示してください。メイン、ガントの時は常に左に表示しておいてください
[x] ガントは横にweekdayの日付を並べ、見積もり工数の長さに応じた長さのバーを表示してください。それにより、いつ何をやるかが一目でわかるようにしてください
[x] 日付で時間帯の行を選択している場合ガントでチケットを選んだ場合にも割り当ててください。シングルクリックで割り当てです。
[x] 検索はタブを分ける必要はなくメインの階層ツリーで検索できるようにしてください
[x] 日次スケジュールの幅が狭いです。画面の半分程度まで広げれるようにして下さい
[x] 日次スケジュールの予定として割り当てれるのは自分のticketだけです。他人のticketは割り当てれないようにする
[x] ガントチャートタブをメインとし、今のメインはEditというタブ名にする。デフォルトはメインタブ表示
[x] ガントのProject選択ドロップダウンメニューが反応しないので修正する
[x] ガントの"表示ボタン"は不要。Project,担当、期間を変更したら自動で更新する
[x] ガントにはStatusも表示する。そして上部に表示するStatusの選択ラジオボタンを設定する
[x] ガント: ラジオボタンにdoingは不要
[x] ガント: デフォルトは自分のみtodoを表示
[x] ガント: 表示対象のticketがない場合、taskも表示しない
[x] 日次: 行にアイテムを割り当てたら、行の選択を解除
[x] 日次: 2行以上に連続してアイテムを割り当てる場合、1行めにはTask,2行目にTicketの内容を記載する
[x] ガント: 開始可能日、納期、見積もり工数、実績工数から業務の順序を計算し、それに基づいて作業日に線を引く。開始可能日は▶️を表示し、納期は🚩、作業日は🔨のマークを入れる。業務順序の計算や作業時間については、旧Verであるa03_Schedule_Manager_v2の仕様をよく確認し、踏襲すること。
[x] 日次: 行の高さを今のやく半分にして下さい。時間のフォントを調整してはみ出さないように
[x] ガント: ticketを右クリックでメニュー表示。メニューの項目はEdit, 開始可能日変更、納期変更、ToDo, Done, Cancel, Regularly, Delete. EditはEditタブに飛びその項目を表示、開始日、納期変更はカレンダーを表示して変更する。Todoなどを押した場合は、そのStatusに変更。Deleteは削除前に確認メッセージ表示。いずれも自分もTicketの場合のみ変更可能
[x] ガント: デフォルトはtodo, 自分を表示。起動時に表示
[x] Edit: テキスト一括入力欄は削除
[x] Edit: 右側のチケット内容の表示は不要。代わりに表の表示スペースを横に広げる
[x] Edit: ベースは自分のみをデフォルトにし、Editを開いた時点で表示
[x] Edit: 日次の表示を取りやめ。ガントだけに表示させる
[x] Edit: 編集はボタンを押さなくても表をエクセルライクに編集できるようにする
[x] Edit: Ticketの編集の改善
    ctrl+enterで下に行を追加
    Statusはクリックで選択肢表示、もしくはalt+上下矢印で選択
    日付はクリックでカレンダー表示
    Statusとカレンダーは自由入力不可
    上下ボタンで優先順位変更しても選択行は選択したままにする
    上下ボタンの優先順位変更は複数選択して移動可能にする
[x] Edit階層ツリーは、階層に薄い線を引き、project1234task,ticketのどれであるかをわかりやすく認識できるようにする
[x] Edit: 階層はわかりやすくなったが、P2以降が並列に並んでいるので、P3〜Ticketも買いそうになるように表示してください
[x] メイン: メインタブ内にある担当者のドロップダウンは不要です。上部のバーにある表示メンバーの情報を使って下さい
[x] 上部バー: 日付の選択欄の左右に前日に戻る、翌日に進む、今日に戻るボタンを作って下さい。ボタンは文字でなくアイコンで表示して下さい。
[x] Edit: 選択がTicketの時は子供を作れないようにする
[x] ADD: Configタブを追加c..して下さい.config.iniの設定を閲覧・変更できるようにして下さい。
[x] ALL: 各アイテムの持っている優先度を順序という名前に変えて下さい。DBは変更不要です。表示を変えて下さい
[x] ガント: 納期が設定されていないチケットについても、納期を内部的に計算して実行予定を表示させて下さい。仮の納期は、①親が同じであるTicketについて順番通りに並べ、後ろから順番に確認する。そして後ろから順番に納期を計算する。例えばTicket1がの見積もり工数が10、実績が0,納期なし、ticket2が見積もり20,実績5,納期3/5の場合、ticket1の納期は3/5から15時間(見積もりー実績)遡った日が納期になる。１日の作業工数はconfigで設定し、例えば5時間であれば、3/5から15時間遡ると、3/5も作業するとして、3/3にはticket2のスタートが必要。つまり3/2がticket1の納期になる。②他のTaskと整合どりをする。ticketを納期が早いものから順番に取り出し、開始可能日より前でなければ、その日を実行日にする。そこから実行にかかる時間分後ろにずらし、次に納期が早いものを取り出し、順番に設定する。開始可能日が引っかかって飛ばしたticketは常に順番を1においてチェックする。③作業日がたとえ納期より遅くなった場合も、その作業日を設定し、色を付けてアラートする。
なお、この仕様について不明点がある場合は、この項目以外を修正してから、仕様の確認をしてから作業する
[x] ガント: 改善をする前にチェックしやすくするため、チケットにマウスオーバーしたら、チケットの内容が表示されるようにする。
[x] ガント: マウスオーバーの表示に、ガントを描くための仮の納期と作業日を表示させるようにする
[x] ガント: ガントの表示テストのため、dbに4/15が納期であるアイテムをたくさん作る
[x] ガント: dbに4/15が納期であるアイテムを追加で作って下さい。今回は、taskを2つ作って下さい。両方ともticketの数は5つ。1つのTaskはどの順番にも開始可能日、納期を設定しない。もう一つは順番が最後のTicketのみに納期を設定する。そして順番が3番目のticketに開始可能日を設定する。
[x] ガント: 順番が最後のアイテムに納期が設定されていない場合、最後のアイテムには、親の納期を設定して下さい。
[x] ガント: チケットに仮の納期設定後に、ticketの実施日を割り当てる作業は、その人が持っているすべてのticketをまとめてやります(親Taskが違うものも一緒にする。表示していないProjectも一緒にする)。その人の1日の作業時間がConfigで設定している時間になるように割り当てるように変更する
[x] ガント: 作業開始日が設定されている場合、同じタスクで順序がそれより後ろのチケットについては、開始可能日がその設定日より遅くなる量にします。例えば
チケット、順番、開始可能日
A 1 3/3
B 2 -
C 3 3/7
D 4 -
の場合、内部的には
A 3/3
B 3/3
C 3/7
D 3/7
と計算するようにする。

続いて、順番を決める場合
チケット、親、順番(納期順で並び替えた内部で計算した順番)、開始可能日、工数
A P1 1 3/3,6
B P1 2 - ,6
C P2 3 - ,6
D P2 4 - , 6
を3/2かから割り当てる場合、
3/2 C
3/3 A
3/4 B
3/5 D
となるように並び替えて下さい。開始可能日で始めれない場合は別の始めれるタスクを実行します
[x] ガント: 作業日表示が修正できたのでtooltipの表示を変える。tooltipには、担当、開始可能日、納期、見積工数/実績工数、Memoを表示させる
[x] ガント: Taskもtooltipを表示させる。納期、親(p1~P4)、(子供の工数を積算した）見積工数/実績工数を表示させる
[x] ガント: Ticketのステータスを変更したら、仮納期などを再計算して反映させる
[x] ガント: 各Taskについて、子供の作業日の最も早い日をそのタスクの作業日として、作業日が早い順に上から並ぶようにして下さい。Taskの順番が入れ替わっても構いません
[x] 日次スケジュール:時間の見栄えを直す。現状以下のようになっています。
04:00~04:15
  ~04:30
~の位置が縦に揃うように表示させて下さい
[x] configのdaily_begin_time, end_timeが日次の開始と終了になるようにしたいでう。これは、その範囲だけ出すのではなく、今の設定のように1日のスケジュール全体がスクロールで見えるようにしつつ、初期の状態でbegin timeが一番上に来るようにスクロールする。またend timeが一番下に来るように行の高さを調整するという設定をして下さい。
[x]日次: 上記の修正で時間がわかりにくくなったので、1時間ごとに線を引いて下さい。04:00~04:15など00分の上に線が欲しいです。また見た目もオシャレにして下さい
[x]ロードマップ: こちらはガントでなくスケジュールです。入力されている開始日と納期の情報をもとにスケジュール表として見やすく配置して下さい。上部に選択欄を用意して下さい。
    - 選択では最初にどのレベルを表示するかを選びます。Project2,3,4,Taskです。
    - 次に親の絞り込みをします。絞り込みがない場合は全部表示します。表示する順番は各アイテムが持っている順番に従います。
    - 親の選択はEditの階層表示を流用して下さい。但しデフォルトではProject4まで見えるようにしておき、それ以下は閉じておいて下さい。中にどんなアイテムがあるか確認したいかもしれないので、開いたら見えるようにしておいて下さい
[x] ロードマップ: 親の絞り込みの見た目はEditと同じにする。色分けや[P1]という書き方など、Editの階層表示はみやすい
[x] ロードマップ: 表示レベルは選択ボタンにする。可愛く
[x] ロードマップ: 縦に項目、横に日程が並ぶように（今のガントのように）表示する。期間の1つのセルは日、週、月で選択できるようにする
[x] ロードマップ: 開始日から納期の範囲に色をつけて、その期間に実施する計画であることを示す。加えて配下のTicket情報を上流に流し、作業実績がある期間に色をつける。計画と実績がわかりやすくなるように表示する
[x] ロードマップ: 表示選択の階層はP4を表示、Taskは表示しない(閉じて見えない)状態を初期状態にする
[x] ロードマップ: 表示期間はデフォルトでその年の会計年度とする。例えば、開いている日が2026/5/20であれば、表示は2026/4/1~2027/3/31をデフォルトとする。
[x] ロードマップ: 列幅をマウス操作で変えれるようにする
[x] ガント: こちらも列幅をマウス操作で変えれるようにする
[x] ロードマップ: 各項目は親ごとに塊を作る。
例えばtaskの時は
project1:
 project2:
  project3:
   project4:
    task1  スケジュールを表示
    task2  スケジュールを表示
できるのであれば、階層表示を参考にする。階層表示を引き伸ばして横にスケジュールを表示してもいい。
[x]ロードマップ: 視認性改善のため、背景色を薄くする。うっすら見える程度
[x]チームログの本日の入力欄をメインの日次の上に移動。縦方向を狭くしてなるべく場所を取らないようにする。
[x]メイン(本日の入力を移動後):本日の入力の連絡事項は、今日と常時の２つにする。今日はその日だけの連絡、常時はずっと表示させる。常時はDBにpermanent_noticesテーブル(Owner, text, updated_at)で保存
[x]チームログ: 上記の変更を受けて、表示領域を修正する
[x]ロードマップ: 項目をダブルクリックで、その項目の修正ができるようにする
[x]ロードマップ: 右クリックメニューで、Editが選択できるように。Editはガントと同様にEditタブに移動してその項目を表示
[x]タブ：並びと表示を変える。並びと表示名は次のとおり。タブの幅が同じになるようにして視認性改善のために縁をつける
表示名/現在のタブ名
Main/メイン（ガント）
Plan/ロードマップ
Edit/Edit
Team/チームログ
Search/検索
Request/依頼
Memo/メモ
Analyze/分析
Version/バージョン
config/config
[x] タブ：オシャレにする
[x] Plan: ダブルクリックはポップアップでの変更画面を出す。Editでダブルクリックした時と同様にする。
[x] Main: todo, done, cancelに加えてRegularlyも選択できるようにする。Regularlyはスケジュールの表示は空白で、項目だけを他のtodoやdoneのように並べる
[x] Edit: エクセルライクに変更できるようにする。キーボード操作で移動でき、移動した場所が工数であれば、数字を打ち込むと変更される。ダブルクリックで選択しなくてもいい。納期はマウスでクリックしたらカレンダーが出る。ステータスもクリックで選択肢が出る。加えて、その場所でALT+上下で変更できる。一度変更してもaltキーを押していればセル移動しない。表の一番したでctrl+enterが押されたら、下に行を追加する。デフォルトは、見積もり工数1,タイトルはなし、statusはtodo、納期や開始日はしていなし
[x] 名前はメールアドレスで管理する。yamada@email.comなど。そしてメールアドレスと表示名の関係を.iniファイルに記載し、アプリではその表示名を使用する。この変更に合わせて、テスト用に準備しているschedule.sqliteのデータも書き換える